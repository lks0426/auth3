# Basic Nginx Dockerfile
FROM nginx:alpine
COPY nginx.conf /etc/nginx/nginx.conf
# For dev, you might copy nginx.dev.conf instead or have it mounted via docker-compose
# COPY nginx.dev.conf /etc/nginx/conf.d/default.conf
