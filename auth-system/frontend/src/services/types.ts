// Placeholder for types used in services (e.g., API request/response payloads)
// This might be an aggregation or re-export of types from ../types/
// Example:
// export * from '../types/auth'; // Re-exporting from a more specific location
// export * from '../types/user';
//
// // Or define service-specific types here if not covered elsewhere
// export interface GenericApiResponse<T> {
//   data: T;
//   message?: string;
//   status: 'success' | 'error';
// }
//
// export interface ErrorResponse {
//   detail: string | { msg: string; type: string }[];
// }

console.log("Service types placeholder");
