import React from 'react';

interface LoadingProps {
  // Add any custom props here, e.g., size, message
}

const Loading: React.FC<LoadingProps> = () => {
  return <div>Loading...</div>;
};

export default Loading;
